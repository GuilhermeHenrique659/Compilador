#teste#<!>

inicio

struct stack {
    bool isValid;
}

function main(a: int): int {
    int array[] = [10,23];
    string name = "teste";
    a = 10;
    b = a + 1;

    name.len();

    compiladores
    IFSULDEMINAS

    stack.isValid = false;

    for (int i = 0; i < 1; i++) {
        if (i == 10 or not i == 1)
        print(array[i]);
        i = i + 1;
    }
}

fim