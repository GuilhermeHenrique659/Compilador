#teste#<!>

inicio

struct stack {
    bool isValid;
}

function main(a: int): int {
    int array[] = [10,23];
    string name = "teste";
    a = 1.5;
    b = a + 1;

    int 10;
    name.len();

    compiladores
    IFSULDEMINAS

    stack.isValid = false;

    for (int i = 1; i < 1; i++) {
        if (i == 10 or not i == 1)
        print(array[i]);
        i = i + 1;
    }
}

fim